


<nav class="navbar navbar-inverse">
    <div class="container-fluid">
        <div class="navbar-header">
            <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#myNavbar">
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>
            <a class="navbar-brand" asp-area="" asp-controller="Home" asp-action="Index">CarsBG</a>
        </div>
        <div class="collapse navbar-collapse" id="myNavbar">
            <ul class="nav navbar-nav">
                <li class="nav-item">
                    <a class="nav-link text-white" asp-area="" asp-controller="Car" asp-action="Index">Search Car</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link text-white" asp-area="" asp-controller="Car" asp-action="Add">Add Car</a>
                </li>

                @if (this.User.Identity.IsAuthenticated)
                {
                    <li class="right">
                        <a class="nav-link text-white" asp-area="" asp-controller="Car" asp-action="MyCars">My Cars</a>
                    </li>
                }
                @if (User.Identity.IsAuthenticated && (User.IsInRole("Administrator") || User.IsInRole("Moderator")))
                {
                    <li class="nav-item">
                        <a class="nav-link text-white" asp-area="Admin" asp-controller="Admin" asp-action="Index">Admin Panel</a>
                    </li>
                }
                <partial name="_LoginPartial" />

            </ul>


        </div>
    </div>
</nav>
